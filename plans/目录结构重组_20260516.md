# 目录结构重组计划

- [x] 盘点现有 `include/`、`src/`、`core/include/` 中的头文件与源码分布
- [x] 明确目标目录结构与迁移映射
- [x] 与主人确认 `engine` / `infra` 的边界
- [x] 移动文件并批量修正 `#include` 路径
- [x] 更新 `CMakeLists.txt` 中的 include 搜索路径
- [x] 执行构建验证并修正遗漏引用

## 当前盘点

### `include/`
- `engine.h`
- `framework.h`
- `logging.h`
- `latency_stats.h`
- `tick_trace.h`
- `static_delegate.h`
- `hash_containers.h`
- `factor/factor_node.h`

### `src/`
- `engine.cpp`
- `logging.cpp`
- `main.cpp`

### `core/include/`
- `queue.h`
- `intrusive_pool.h`
- `mmap_util.h`
- `shared_spsc_ring.h`
- `protocol.h`
- `market_snapshot.h`
- `tick_window_snapshot.h`
- `symbol_manager.h`
- `order_manager.h`
- `core_state.h`
- `shm_snapshot.h`
- `simple_matching_engine.h`

## 初步迁移思路

### 目标目录结构

```text
engine/
  include/
  src/

infra/
  include/
  src/   # 如后续需要实现文件再放

core/
modules/
```

### 迁移映射

#### `engine/include/`
- `engine.h`
- `framework.h`
- `logging.h`
- `latency_stats.h`
- `tick_trace.h`

#### `engine/src/`
- `engine.cpp`
- `logging.cpp`
- `main.cpp`

#### `infra/include/`
- `hash_containers.h`
- `static_delegate.h`
- `queue.h`
- `intrusive_pool.h`
- `mmap_util.h`
- `shared_spsc_ring.h`

### 保持在 `core/include/` 的核心域头文件
- `protocol.h`
- `market_snapshot.h`
- `symbol_manager.h`
- `order_manager.h`
- `core_state.h`
- `shm_snapshot.h`
- `tick_window_snapshot.h`
- `simple_matching_engine.h`

## 已确认边界

- 顶层父目录命名使用 `engine/` 与 `infra/`
- `engine` / `infra` 是与 `core/` 同级的物理目录，不是 `include/` 下的子目录
- `core/` 暂时保留为核心领域层，不并入 `infra/`

## 实际执行结果

### 已迁移到 `engine/include/`
- `engine.h`
- `framework.h`
- `logging.h`
- `latency_stats.h`
- `tick_trace.h`

### 已迁移到 `engine/src/`
- `engine.cpp`
- `logging.cpp`
- `main.cpp`

### 已迁移到 `infra/include/`
- `hash_containers.h`
- `static_delegate.h`
- `queue.h`
- `intrusive_pool.h`
- `mmap_util.h`
- `shared_spsc_ring.h`

### 已完成的兼容修复
- 根 `CMakeLists.txt`、`trade_gateway/CMakeLists.txt`、`hft_md/CMakeLists.txt`、`hft_ba_md/CMakeLists.txt` 已更新 include/source 路径
- 业务模块与测试中的旧相对 `#include` 已批量修正
- `cmake --build build -j4` 已通过

### 未同步的非阻塞残留
- 文档与 repo skill 中仍有旧路径文案，如 `src/engine.cpp`、`include/engine.h`
- 这些不影响构建与运行，后续可按需要统一更新
