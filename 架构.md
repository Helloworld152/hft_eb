
# 高频/中低频统一量化系统架构设计文档（插件化 + DAG + 多线程模型）

## 1. 设计目标

构建一套同时支持：

- 高频交易（微秒级延迟）
- 中低频多策略系统
- 大规模因子计算（研究/回测）

的统一架构系统，并满足：

- 插件化扩展（动态库加载）
- 配置驱动
- 可控线程模型
- 高性能（cache-friendly / 低延迟）
- 因子计算复用（在线 + 离线）

---

## 2. 核心设计原则

### 2.1 三层职责分离

| 层 | 职责 |
|--|--|
| Event Bus | 事件传播 |
| State Layer | 状态维护（增量） |
| Factor DAG | 计算与依赖管理 |

---

### 2.2 三种计算模式

| 模式 | 用途 |
|--|--|
| Push（事件驱动） | 状态更新 |
| Pull（按需计算） | 策略决策 |
| Batch（块计算） | 因子/回测 |

---

### 2.3 三类执行器

| 执行器 | 场景 |
|--|--|
| LowLatencyExecutor | 高频 |
| StrategyExecutor | 中低频 |
| ComputeExecutor | 因子计算 |

---

## 3. 系统总体架构

### 3.1 实时链路

```text
Tick → EventBus → State → mark dirty → Trigger → DAG eval → Strategy → Order
````

---

### 3.2 离线链路

```text
Historical Data → Block → ComputeExecutor → Factor DAG → Output
```

---

## 4. 模块设计

---

# 4.1 Event Bus（同步事件总线）

## 设计目标

* 极低延迟
* 无锁
* 顺序执行
* 函数指针调用

---

## 接口

```cpp
template <typename Event>
class SyncBus {
public:
    using Handler = void(*)(void*, const Event&);

    struct Subscriber {
        void* ctx;
        Handler fn;
    };

    void subscribe(void* ctx, Handler fn);
    void publish(const Event& ev);

private:
    std::vector<Subscriber> subscribers_;
};
```

---

## 特点

* 无虚函数
* 无分配
* cache-friendly
* 高频友好

---

# 4.2 State Layer（状态层）

## 职责

* 增量更新
* O(1)操作
* 无复杂计算
* 提供因子输入

---

## 核心状态

### BookState

```cpp
class BookState {
    double bid_px;
    double ask_px;
    double bid_qty;
    double ask_qty;
};
```

---

### RollingWindow

```cpp
template <size_t N>
class RollingWindow {
    double buf[N];
    double sum;
};
```

---

### BarBuilder

```cpp
class BarBuilder {
    double open, high, low, close;
};
```

---

## 特点

* 不做复杂逻辑
* 不依赖 DAG
* 高频路径核心

---

# 4.3 Factor DAG（核心）

## 职责

* 表达依赖关系
* 避免重复计算
* lazy evaluation
* dirty propagation

---

## 节点接口

```cpp
class IFactorNode {
public:
    virtual void mark_dirty() = 0;
    virtual bool is_dirty() const = 0;
    virtual double eval() = 0;
};
```

---

## 节点属性

```cpp
bool dirty;
double cached_value;
uint64_t version;
```

---

## 执行逻辑

```text
eval():
  if dirty:
    eval children
    recompute
  return cached
```

---

## DAG管理

```cpp
class FactorGraph {
    add_node(...)
    add_edge(child → parent)
    mark_dirty(node)
};
```

---

# 4.4 Trigger（策略触发器）

## 职责

控制计算时机，而不是事件驱动计算。

---

## 类型

* TickTrigger
* IntervalTrigger
* BarCloseTrigger
* CustomTrigger

---

## 接口

```cpp
class ITrigger {
public:
    virtual bool should_fire(...) = 0;
};
```

---

# 4.5 Strategy Engine

## 职责

* 拉取因子
* 生成信号
* 决策

---

## 执行方式

```cpp
if (trigger.fire()) {
    auto signal = factor.eval();
    decide(signal);
}
```

---

# 4.6 Risk Engine

## 职责

* 订单检查
* 仓位限制
* 风险控制

---

## 模型

责任链模式：

```cpp
for (risk : chain) {
    if (!risk.check(order)) reject;
}
```

---

# 4.7 Gateway

## 职责

* 发送订单
* 接收回报

---

## 特点

* 高频：低延迟 TCP / kernel bypass
* 模拟：mock exchange

---

# 4.8 Compute Engine（批量因子计算）

## 职责

* 批量计算
* 并行执行
* 回测支持

---

## 接口

```cpp
class IBlockFactor {
public:
    virtual void compute(const Block&, Output&) = 0;
};
```

---

## 执行器

```cpp
class ComputeExecutor {
    parallel_for(...)
};
```

---

## 调度方式

* 按 symbol 分块
* 按时间分块

---

# 5. 线程模型设计

---

## 5.1 高频模式

```text
Core2: MarketData
Core4: Strategy + Risk + Gateway
```

特点：

* SPSC
* 单线程策略
* 无锁
* 低抖动

---

## 5.2 中低频模式

```text
EventBus → StrategyPool → GatewayPool
```

特点：

* 多线程
* 任务调度
* 插件友好

---

## 5.3 因子计算模式

```text
Reader → ComputePool → Writer
```

特点：

* 并行计算
* 数据分块
* 高吞吐

---

# 6. 插件化设计

---

## 插件类型

* Strategy
* MarketData
* Risk
* Gateway
* Factor（可选）

---

## 工厂函数

```cpp
extern "C" IStrategy* create();
```

---

## PluginManager

职责：

* dlopen
* dlsym
* 生命周期管理

---

# 7. 配置驱动

---

## 示例

```yaml
runtime:
  mode: hft

executors:
  low_latency:
    cpu_cores: [2,4]

strategies:
  - name: mm
    lib: ./mm.so
    executor: low_latency
```

---

# 8. 数据流总结

---

## 实时

```text
Tick → State → Dirty → Trigger → DAG → Strategy → Order
```

---

## 离线

```text
Block → Compute → Factor → Output
```

---

# 9. 性能关键点

---

## 高频

* 无锁
* cache-friendly
* 无分配
* SPSC
* bind core

---

## 中低频

* 线程池
* 解耦
* 异步

---

## 因子计算

* block计算
* SIMD
* NUMA优化

---

# 10. 设计优势

---

## 优点

* 高频路径极简
* 因子可复用
* 插件化扩展
* 支持多模式
* 易测试

---

## 避免的问题

* tick驱动全量计算 ❌
* 重复计算 ❌
* 状态与计算耦合 ❌
* 高频路径复杂化 ❌

---

# 11. 核心总结

---

> **Event Bus 推进状态**
> **DAG 控制计算**
> **Trigger 控制时机**
> **Compute Engine 负责规模化计算**

---

# 12. 下一步建议

---

建议你按这个顺序实现：

1. State Layer
2. DAG（dirty + cache）
3. Trigger
4. Strategy Engine
5. Compute Engine
6. PluginManager

---

# 最终一句话总结

> 这套系统的本质是：
> **用 DAG 把“数据流”和“计算流”解耦，用线程模型把“延迟”和“吞吐”分层控制。**

```
```

---

## 11. Core 下沉边界（当前阶段）

为避免 core 过度膨胀，当前阶段仅下沉三类“权威状态”：
- 持仓 (Position)
- 订单 (Order)
- 账户资金/冻结 (Account)

其余模块保持在上层：策略、组合、回测、监控/UI、数据回放等。
